{% extends 'base.html.twig' %}

{% block title %}Edit Allergens{% endblock %}

{% block body %}
    <h1>Edit Allergens</h1>

    {{ include('allergens/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('allergens_index') }}">back to list</a>

    {{ include('allergens/_delete_form.html.twig') }}
{% endblock %}
